initialise local git repo
`git init`

check for changes to repo
`git status`

add file to 'stage'
`git add <filname>`

commit changes
`git commit -m "useful note"`

view commit history
`git log --oneline`

